// import { Student } from "../students/student.model";

import { Student } from "../students/student.model";

export class Enrollment {
    enrollment_id: number=0;
    student_id: number=0;
    course_id: number=0;
    enrollment_date: string='';
    // student:Student = new Student();
  }
  